
//test:
//if (5 <= 1) {
//  Child u;
//  v = u.isLocatedAt;
//  String s = "Blabla";
//  s.equals("foo");

//}
boolean firstEncounter();


bogus:
// comments show what must be generated
if (true) {
  Activity activity;

  // palAgent.activity.setSingleValue("status", "gameProposed");
  activity.status = "gameProposed";

  // palAgent.activity.getSingleValue("status") == "gameProposed";
  bool = (activity.status == "gameProposed");

  // String daType = palAgent.firstEncounter() ? "a" : "b"
  daType = firstEncounter() ? "a" : "b";

  // isEqual((Rdf)user.getSingleValue("isLocatedAt"), "Home");
  // Issue 1: single Value vs. Multiple Value, for set an get
  // Issue 2: provide the right cast based  on the range
  Child user; user.isLocatedAt == "<dom:Home>"; // should be a const somewhere.
  // currently: isEqual(user.getValue("isLocatedAt"), "Home");

  // functions coming from the Agent are called via inheritance, not using
  // palAgent.emitDA .newTimeout for example

  // boolean b = user.getSingleValue("<dom:firstName>").equals("John")
  b = (user.forename == "John");

  // user.setSingleValue("<dom:firstName>", "John")
  user.forename = "John";

  Object foo;
  foo.slot = 1;

}
