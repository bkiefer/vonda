a = 1; b = 2; c = 3; Agent user;

rule_one:
if (((a != b) || (b != c)) && (a == c)) {
  bla = 0;
  rule_one_a:
  if (b == b && ! (a + b) < (b + c)) {
    blo = 0;
  }
}

rule_two:
if (! user)
  bli = 0;
