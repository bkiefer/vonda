
/*@public class SecondRule extends RuleUnit {@*/

public void testTest(String a, var b){

ask_for_role:
if (game.status == startSession && ! myLastSA <= #ChoiceQuestion(Role)) {
  if (gameLogic.isTurnBased()) {
    emitDA(#ChoiceQuestion(Role));
  } else {
    gameLogic.newRound();
  }
}
}
